# Development / VCS metadata
/.git
/.github
/.cursor
/.vscode
/.idea
/.wordpress-org
/.gitignore
/.gitattributes
/.distignore

# GitHub-only documentation (not for WordPress.org)
/README.md

# Composer / PHPCS tooling (dev-only for this plugin)
/vendor
/composer.json
/composer.lock
/phpcs.xml
/phpcs.xml.dist
/.phpcs.xml
/.phpcs.xml.dist

# Testing infrastructure (dev-only, not needed in production)
/tests
/phpunit.xml.dist
/jest.config.js
/package.json
/package-lock.json

# Development scripts (exclude all PowerShell scripts from zip)
*.ps1

# Node / build artifacts
/node_modules

# OS/editor junk
.DS_Store
Thumbs.db

# Release artifacts
*.zip
