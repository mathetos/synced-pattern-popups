# Development / VCS metadata
/.git
/.github
/.cursor
/.vscode
/.idea
/.wordpress-org
/.gitignore
/.distignore

# Composer / PHPCS tooling (dev-only for this plugin)
/vendor
/composer.json
/composer.lock
/phpcs.xml
/phpcs.xml.dist
/.phpcs.xml
/.phpcs.xml.dist

# Testing infrastructure (dev-only, not needed in production)
/tests
/phpunit.xml.dist
/jest.config.js
/package.json
/package-lock.json

# Development scripts
/release.ps1
/fix-phpcs.ps1
/fix-phpcs-v2.ps1

# Node / build artifacts
/node_modules

# OS/editor junk
.DS_Store
Thumbs.db

# Release artifacts
*.zip
